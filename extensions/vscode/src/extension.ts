// LastGate VS Code Extension
// Activation: when workspace contains .lastgate.yml

export function activate(context: any) {
  console.log("LastGate extension activated");

  // Status bar, sidebar, diagnostics, and commands
  // are registered here when running in VS Code context
}

export function deactivate() {
  // Cleanup
}
