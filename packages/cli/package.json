{
  "name": "lastgate",
  "version": "0.3.0",
  "description": "AI agent commit guardian — pre-flight checks for AI-generated code",
  "bin": {
    "lastgate": "dist/lastgate.js"
  },
  "scripts": {
    "build": "bun build bin/lastgate.ts --outdir dist --target node",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "latest",
    "@lastgate/engine": "workspace:*"
  },
  "files": ["dist/"],
  "keywords": ["lastgate", "ai", "agent", "commit", "guardian", "security", "lint", "cli"],
  "license": "MIT",
  "author": "Aaron Character"
}
